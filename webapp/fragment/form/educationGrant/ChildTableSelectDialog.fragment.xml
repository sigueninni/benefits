<core:FragmentDefinition
	xmlns="sap.m"
	xmlns:core="sap.ui.core"
    xmlns:customData="http://schemas.sap.com/sapui5/extension/sap.ui.core.CustomData/1"
    
    >
	<TableSelectDialog
		noDataText="{i18n>NochilFound}"
		title="{i18n>SelectAChild}"
		search="onChildSearch"
		confirm="onConfirmChild"
		cancel="handleClose"
		items="{
			path : '/ChildsVHSet',
			parameters: {operationMode: 'Client'},
			sorter : {
				path : 'Fanam',
				descending : false
			}
		}">
		<ColumnListItem>
			<cells>
			<!-- 	<ObjectIdentifier
					title="{Name}"
					text="{ProductId}" /> -->
				<Text text="{Fanam}" />
				<Text text="{Favor}" />
				<Text text="{ path: 'Fgbdt', type: 'sap.ui.model.type.Date', formatOptions: { style: 'medium', strictParsing: true, relative: false, pattern: 'dd/MM/yyyy', UTC: true }}"/>
				<!-- <Text text="{Width} x {Depth} x {Height} {DimUnit}" />
				<ObjectNumber
					number="{WeightMeasure}"
					unit="{WeightUnit}"
					state="{
						path: 'WeightMeasure',
						formatter: 'sap.m.sample.TableSelectDialog.Formatter.weightState'
					}" />
				<ObjectNumber
						number="{
							parts:[{path:'Price'},{path:'CurrencyCode'}],
							type: 'sap.ui.model.type.Currency',
							formatOptions: {showMeasure: false}
						}"
						unit="{CurrencyCode}" /> -->
			</cells>
		</ColumnListItem>
		<columns>
			<Column width="auto">
				<header>
					<Text text="{i18n>lastName}" />
				</header>
			</Column>
				<Column width="auto">
				<header>
					<Text text="{i18n>firstName}" />
				</header>
			</Column>
							<Column width="auto">
				<header>
					<Text text="{i18n>dateOfBirth}" />
				</header>
			</Column>
			<!-- <Column
				minScreenWidth="Tablet"
				demandPopin="true" >
				<header>
					<Text text="Supplier" />
				</header>
			</Column>
			<Column
				minScreenWidth="Desktop"
				demandPopin="true"
				hAlign="End">
				<header>
					<Text text="Dimensions" />
				</header>
			</Column>
			<Column
				minScreenWidth="Desktop"
				demandPopin="true"
				hAlign="Center">
				<header>
					<Text text="Weight" />
				</header>
			</Column>
			<Column hAlign="End">
				<header>
					<Text text="Product" />
				</header>
			</Column> -->
		</columns>
	</TableSelectDialog>
</core:FragmentDefinition>